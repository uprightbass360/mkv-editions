# generator output
out/
sample/
*.m2ts
*.mkv
chapters.xml
build.sh

__pycache__/
*.pyc
.pytest_cache/
pytest-of-*/
.superpowers/

# test discs (large; never commit)
*.iso
# large local test disc dumps kept on disk, never commit
/Avatar.2009.*/

# leaked test-fixture temp dirs (test runs / sandbox wrote these to the repo root)
/mkved-sample-*/
/ez-*/
/fb-*/
/ri-*/
/proj-*/
/poster-*/
# node V8 compile cache
/node-compile-cache/

# repo-root probe cache from manual --cache runs
cache/

# electron/sveltekit app build artifacts
app/node_modules/
app/dist-electron/
app/renderer/build/
app/renderer/.svelte-kit/
# vite/vitest write a cache dir here even though deps hoist to app/node_modules/
app/renderer/node_modules/
